<h2><%= "##{@tag.name}"%></h2>

<section>
  <%= line_chart @data.group_by_day(:created_at, "avg", "value"), :min => 18 %>
</section>

<section>
  <%= render :partial => 'data/datalist' %>
</section>

<section>
  <p><%= link_to "Go back", data_path %></p>
</section>
